
```js
<LoginButton
    displayName="Facebook"
    provider="fProvider"
    className="facebook"
    onClick={() => alert('fProvider')} />
```

```js
<LoginButton
    displayName="Google"
    provider="gProvider"
    className="google"
    onClick={() => alert('gProvider')} />
```

```js
<LoginButton
    displayName="Twitter"
    provider="twProvider"
    className="twitter"
    onClick={() => alert('twProvider')}  />
```

```js
<LoginButton
    displayName="Github"
    provider="ghProvider"
    className="github"
    onClick={() => alert('ghProvider')}  />
```
